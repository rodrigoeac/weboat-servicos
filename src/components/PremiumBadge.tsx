export function PremiumBadge() {
  return (
    <span className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-heading font-semibold text-white bg-gradient-to-r from-ocean-deep to-wave-blue">
      Premium
    </span>
  );
}
